# Audit Export Bundle

## Overview

This is a static audit log export bundle generated by WebWaka for verification purposes.

**Important:** This bundle is evidence for verification — not monitoring, not surveillance, not enforcement.

## Bundle Information

| Field | Value |
|-------|-------|
| Export ID | exp_2026010912345 |
| Generated | 2026-01-09T12:00:00Z |
| Tenant | Demo Retail Store |
| Domain | demo-retail.webwaka.com |
| Suite | Commerce |
| Time Range | 2026-01-01 to 2026-01-09 |
| Entries | 1,247 |

## Contents

```
audit-export-demo-retail-20260109/
├── README.md              (this file)
├── manifest.json          (file index with checksums)
├── metadata/
│   ├── scope.json         (export scope definition)
│   ├── governance.json    (governance assertions)
│   └── integrity.json     (cryptographic hashes)
├── audit-logs/
│   ├── entries.json       (audit entries - JSON format)
│   └── entries.csv        (audit entries - CSV format)
└── verification/
    ├── sha256-manifest.txt (file hashes)
    └── signature.sig       (bundle signature)
```

## Verification Instructions

### Step 1: Verify File Integrity

```bash
# Verify individual file hashes
sha256sum -c verification/sha256-manifest.txt
```

### Step 2: Confirm Bundle Hash

```bash
# Compute bundle hash and compare to integrity.json
cat verification/sha256-manifest.txt | sha256sum
```

### Step 3: Review Audit Entries

Use `entries.json` for programmatic analysis or `entries.csv` for spreadsheet review.

## Data Privacy Notice

- IP addresses are redacted
- User IDs are anonymized (not emails)
- Sensitive metadata may be filtered based on scope
- PII is not included unless specifically authorized

## Governance Context

This export was generated under WebWaka v2-FROZEN governance:

- Business logic is immutable
- Commerce Boundary is enforced
- Append-only discipline is active
- Tenant isolation is architecturally enforced

## Support

For questions about this export, contact WebWaka governance team through the original request channel.

---

*WebWaka Audit Export • Evidence Bundle for Verification*
